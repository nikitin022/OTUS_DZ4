import { PagePlaceholder } from '../../../components/ui/PagePlaceholder';

/** Экран профиля донора и онбординга (FR-1). */
export function ProfilePage() {
  return (
    <PagePlaceholder
      title="Профиль донора"
      description="Здесь появится профиль: группа крови, резус-фактор, радиус поиска и настройки уведомлений."
    />
  );
}
