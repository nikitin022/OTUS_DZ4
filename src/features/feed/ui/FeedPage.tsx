import { PagePlaceholder } from '../../../components/ui/PagePlaceholder';

/** Экран живой ленты потребностей (FR-3). */
export function FeedPage() {
  return (
    <PagePlaceholder
      title="Лента потребностей"
      description="Здесь появится живая лента заявок: группа крови, объём, срочность, прогресс и автообновление."
    />
  );
}
